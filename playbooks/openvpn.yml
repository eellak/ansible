---

- hosts: adminvm.ellak.gr
  become: true

  vars_files:
    - "{{ protected_storage }}/adminvm.ellak.gr/openvpn.yml"

  vars:
    - openvpn_clients:
      - pensieve.ellak.gr
      - ma.ellak.gr
      - mail1.ellak.gr
      - nick.user

  roles:
    - openvpn-server

  tags:
    - server

- hosts: vpn
  become: true

  vars:
    - openvpn_dns: yes

  roles:
    - openvpn-client

  tags:
    - clients
